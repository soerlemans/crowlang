module main

import (
		"io";
		fs = "filesystem"
)

interface Cast {
  fn integer() -> Int
  fn string() -> String
}

struct Pair {
  m_first: int
}


fn main() {
	let var = +10 + -30
	let var = true

	return 0
}
